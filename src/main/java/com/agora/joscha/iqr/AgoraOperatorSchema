{
    "title": "Agora Operator",
    "type": "object",
    "properties": {
        "id": {
            "type": "number"
        },
        "rowcount": {
            "type: "string",
        },
        "input": {
            "type": "array",
            "items": {
                "type": "number",
                "description": "contains the id's of the input operator(s)"
            }
        },
        "operator": {
            "type": "string",
            "enum": ["SOURCE", "UNION", "SORT", "POJECT", "LIMIT", "INNER-JOIN", "LEFT-JOIN", "RIGHT-JOIN", "OUTER-JOIN", "INTERSECTION", "GROUPBY", "FILTER", "DISTINCT", "AGGREGATE"]
        }


    }


}